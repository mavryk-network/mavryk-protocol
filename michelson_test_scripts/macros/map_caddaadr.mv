parameter unit;
storage (pair (pair nat (pair nat (pair (pair (pair (nat %p) (mumav %value)) nat) nat))) nat);
code { MAP_CDADDAADR @new_storage %value { PUSH mumav 1000000 ; ADD } ;
       NIL operation ; SWAP; SET_CAR };
