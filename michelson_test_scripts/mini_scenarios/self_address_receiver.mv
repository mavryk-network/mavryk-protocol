# See self_address_sender.mv
parameter (lambda unit address);
storage unit;
code {
       UNPAIR;
       UNIT;
       EXEC;
       SELF_ADDRESS;
       ASSERT_CMPEQ;
       NIL operation;
       PAIR
     }
