parameter (pair bytes (contract bytes)) ;
storage unit ;
code { UNPAIR ;
       DIP { NIL operation };
       UNPAIR ;
       DIP { PUSH mumav 0 };
       TRANSFER_TOKENS;
       CONS;
       PAIR; }
