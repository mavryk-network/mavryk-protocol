parameter address ;
storage (option (pair (pair mumav mumav) (pair (pair address address) address )));
code { CAR ;
       UNIT;
       VIEW "step_constants" (pair (pair mumav mumav) (pair (pair address address) address )) ;
       NIL operation ;
       PAIR } ;

