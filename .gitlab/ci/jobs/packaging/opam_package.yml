# This file was automatically generated, do not edit.
# Edit file ci/bin/main.ml instead.

opam:bls12-381:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: bls12-381
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-accuser-PtAtLas:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-accuser-PtAtLas
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-accuser-PsParisC:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-accuser-PsParisC
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-accuser-PtParisB:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-accuser-PtParisB
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-alcotezt:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-alcotezt
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-baker-PtAtLas:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-baker-PtAtLas
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-baker-PsParisC:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-baker-PsParisC
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-baker-PtParisB:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-baker-PtParisB
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-client:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-client
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-codec:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-codec
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-crawler:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-crawler
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-dac-client:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-dac-client
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-dac-node:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-dac-node
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-dal-node:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-dal-node
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-distributed-internal:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-distributed-internal
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-distributed-lwt-internal:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-distributed-lwt-internal
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-injector:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-injector
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-internal-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-internal-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-l2-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-l2-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-node:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-node
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-node-config:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-node-config
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-proto-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-proto-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-protocol-000-Ps9mPmXa-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 3 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 3 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 3 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-protocol-000-Ps9mPmXa-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-protocol-001-PtAtLas-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-protocol-001-PtAtLas-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-protocol-019-PtParisB-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-protocol-019-PtParisB-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-protocol-020-PsParisC-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-protocol-020-PsParisC-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-protocol-alpha-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-protocol-alpha-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-protocol-compiler:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-protocol-compiler
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-proxy-server:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-proxy-server
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-rpc-process:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-rpc-process
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-shell-libs:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-shell-libs
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-signer:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-signer
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-node:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-node
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-node-PtAtLas:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-node-PtAtLas
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-node-PsParisC:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-node-PsParisC
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-node-PtParisB:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-node-PtParisB
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-node-alpha:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-node-alpha
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-node-lib:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-node-lib
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-wasm-debugger:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 1 minute
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 1 minute
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 1 minute
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-wasm-debugger
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-wasm-debugger-lib:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-wasm-debugger-lib
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-smart-rollup-wasm-debugger-plugin:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-smart-rollup-wasm-debugger-plugin
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:octez-version:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: octez-version
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-benchmark:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-benchmark
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-client-demo-counter:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-client-demo-counter
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-client-genesis:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-client-genesis
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-dac-client-lib:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-dac-client-lib
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-dac-lib:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-dac-lib
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-dac-node-lib:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 2 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 2 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 2 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-dac-node-lib
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-dal-node-lib:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-dal-node-lib
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-dal-node-services:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-dal-node-services
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-openapi:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-openapi
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-protocol-000-Ps9mPmXa:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 4 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 4 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 4 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-protocol-000-Ps9mPmXa
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-protocol-001-PtAtLas:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-protocol-001-PtAtLas
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-protocol-019-PtParisB:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-protocol-019-PtParisB
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-protocol-020-PsParisC:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-protocol-020-PsParisC
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-protocol-alpha:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-protocol-alpha
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-protocol-demo-counter:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-protocol-demo-counter
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-protocol-demo-noops:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-protocol-demo-noops
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-protocol-genesis:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-protocol-genesis
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:mavryk-proxy-server-config:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 6 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 6 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 6 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: mavryk-proxy-server-config
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2

opam:tezt-tezos:
  image: ${build_deps_image_name}:runtime-prebuild-dependencies--${build_deps_image_version}
  stage: packaging
  tags:
  - gcp
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" && $TZ_SCHEDULE_KIND == "EXTENDED_TESTS"
    when: delayed
    start_in: 7 minutes
  - if: $CI_MERGE_REQUEST_LABELS =~ /(?:^|,)ci--opam(?:$|,)/
    when: delayed
    start_in: 7 minutes
  - changes:
    - '**/dune'
    - '**/dune.inc'
    - '**/*.dune.inc'
    - '**/dune-project'
    - '**/dune-workspace'
    - '**/*.opam'
    - .gitlab/ci/jobs/packaging/opam:prepare.yml
    - .gitlab/ci/jobs/packaging/opam_package.yml
    - manifest/manifest.ml
    - manifest/main.ml
    - scripts/opam-prepare-repo.sh
    - scripts/version.sh
    if: $CI_PIPELINE_SOURCE == "merge_request_event" && $GITLAB_USER_LOGIN == "nomadic-margebot"
    when: delayed
    start_in: 7 minutes
  - when: never
  needs:
  - opam:prepare
  dependencies:
  - opam:prepare
  cache:
    key: opam-sccache
    paths:
    - _build/_sccache
  before_script:
  - eval $(opam env)
  - mkdir -p $CI_PROJECT_DIR/opam_logs
  - . ./scripts/ci/sccache-start.sh
  script:
  - opam remote add dev-repo ./_opam-repo-for-release
  - opam install --yes ${package}.dev
  - opam reinstall --yes --with-test ${package}.dev
  after_script:
  - sccache --stop-server || true
  - eval $(opam env)
  - OPAM_LOGS=opam_logs ./scripts/ci/opam_handle_output.sh
  variables:
    RUNTEZTALIAS: "true"
    package: tezt-tezos
    SCCACHE_DIR: $CI_PROJECT_DIR/_build/_sccache
    RUSTC_WRAPPER: sccache
    SCCACHE_ERROR_LOG: $CI_PROJECT_DIR/opam_logs/sccache.log
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_LOG: debug
  artifacts:
    expire_in: 1 week
    paths:
    - opam_logs/
    when: always
  retry: 2
